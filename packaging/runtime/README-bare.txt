Aeterna bare binary
===================

This archive contains only the Aeterna executable. Install an ABI-compatible Qt 6
runtime plus the required Qt Quick, Qt Quick Controls, Qt Labs Platform, and platform
plugin modules before running it. Most users should download the portable package
instead.
