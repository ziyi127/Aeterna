name: aeterna

description: "Aeterna Runtime (monitor-only)"
publish_to: 'none'
version: 1.0.0-test.5+5

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  ntp: ^2.0.0
  window_manager: ^0.4.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/aeterna_logo.png
